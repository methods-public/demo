#
# This file is generated by Chef
#
<%
def generate(conf, ind = 0)
  conf.map do |k, v|
    if v.is_a?(Hash)
      "#{' ' * ind}#{k} {\n" + generate(v, ind + 2) + "#{' ' * ind}}\n"
    else
      "#{' ' * ind}#{k} #{v}\n"
    end
  end.join('')
end
%>
<%= generate(@conf) %>
